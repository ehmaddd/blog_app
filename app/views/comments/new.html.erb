<%= form_with(model: @comment, url: post_comments_path(@post), local: true) do |form| %>
  <div>
    <%= form.label :text %>
    <%= form.text_area :text %>
  </div>
  <div>
    <%= form.submit "Create Comment" %>
  </div>
<% end %>
