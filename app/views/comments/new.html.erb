<%= form_with(model: [@post, @comment], url: user_post_comments_path(@post.author, @post)) do |form| %>
  <div>
    <%= form.text_area :text, placeholder: 'Enter your comment here' %>
    <%= form.submit 'Create Comment' %>
  </div>
<% end %>
